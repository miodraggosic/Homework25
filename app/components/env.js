const localPort = "http://localhost:3000/";

const apiCandidates = `${localPort}candidates`;
const apiCategories = `${localPort}categories`;
console.log(apiCandidates, apiCategories);

export { localPort, apiCandidates, apiCategories };
